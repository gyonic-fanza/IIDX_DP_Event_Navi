/**
 * イベントが「新着」として扱われる日数のしきい値（単位：日）
 * これより最近に更新されたイベントには「✨」マークを付けるなど、視覚的に強調する。
 */
export const NEW_EVENT_THRESHOLD_DAYS = 3;

/**
 * イベントが「緊急」として扱われる日数のしきい値（単位：日）
 * 開催までこの日数を下回ると、⚠️マークなどで視覚的に注意喚起する。
 */
export const URGENT_THRESHOLD_DAYS = 3;
